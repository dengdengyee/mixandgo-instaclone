<% if user_signed_in? && post.liked_by?(current_user)  %>
  <%= button_to "", post_likes_path(post), class: "post-actions-unlike", method: :delete, format: "turbo_stream" %>
<% else %>
  <%= button_to "", post_likes_path(post), class: "post-actions-like", format: "turbo_stream" %>
<% end %>
